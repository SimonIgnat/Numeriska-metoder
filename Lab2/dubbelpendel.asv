u0 = [2 0 0.8 0];

T= 5;
n = 1000;
h = T/n
phi1 = zeros(1,n);
phi2 = zeros(1,n);

[T_out, Y_out] = ode45(@fpendel,[0 5],u0);


plot(T_out,Y_out(:,1),'r',T_out,Y_out(:,3));



for k=1:length(Y_out)
plot([-L L],[0,0],[0 L*sin(Y_out(k,1))],[0 -L*cos(P(k))],’-o’);
axis equal
axis(1.2*[-L L -L L]);
drawnow
end